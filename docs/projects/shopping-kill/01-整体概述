搭建项目：

顶级项目，管理是哪个字项目：sever、api、model

然后将redis、mysql、zookeepoer、rabbitMQ等中间间本地部署并启动。

熟悉下老晁的mybatis逆向工程 ： mybatisPlus

业务难点：

1. 后台返回的商品是否支持秒杀标志位，前置直接利用该标志位来判断
2. 在获取商品详情和抢购按钮时，请求到达后台，应该是不信任的，仍然要校验商品是否在抢购列表中。还要兼容库存检查。
3. 如果是mq做缓冲，什么时候生成订单的？
4. 私信队列的机制是什么
5. 如果更好的保证流量前置
6. 